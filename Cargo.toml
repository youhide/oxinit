[workspace]
resolver = "2"
members = [
    "crates/listen-probe",
    "crates/notify-probe",
    "crates/oxctl",
    "crates/oxinit",
    "crates/oxinit-cgroup",
    "crates/oxinit-graph",
    "crates/oxinit-ipc",
    "crates/oxinit-log",
    "crates/oxinit-service",
    "crates/oxinit-unit",
    "crates/oxinit-user",
    "crates/oxlogd",
    "crates/xtask",
]

[workspace.package]
edition = "2021"
rust-version = "1.95"
license = "MIT OR Apache-2.0"
repository = "https://github.com/youhide/oxinit"
publish = false

[workspace.dependencies]
# basic-toml over toml: it pulls serde and nothing else, where toml adds
# serde_spanned, toml_datetime, and a parser stack. Units need none of that —
# no datetimes, no spans, no format-preserving edits — and this tree ends up
# inside PID 1.
basic-toml = "0.1"
libc = "0.2"
rustix = "1.1"
serde = "1"
# JSON on the control socket. ARCHITECTURE argues the choice: inspectable with
# socat while the message set is still changing matters more than bytes on the
# wire for a socket carrying a few messages a minute.
serde_json = "1"
thiserror = "2"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true

# Never "abort". An abort in PID 1 is a kernel panic; unwinding gives
# catch_unwind at the event loop boundary a chance to recover.
panic = "unwind"

[profile.dev]
panic = "unwind"
